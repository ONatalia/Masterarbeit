/**
 * This package contains all classes that are necessary for the Greifarm domain.
 * As this domain was explored rather early, it uses little ''generic'' 
 * dialogue management classes and instead relies on it's own implementation,
 * which is contained in this package, along with a main class 
 * (GreifarmExperiment) which starts up the greifarm and allows to test 
 * different ASR smoothing conditions.
 */
package org.cocolab.inpro.domains.greifarm;

