/**
 * A collection of IU "sinks". 
 * The IU modules (or PushBuffers) in this package accept 
 * IUs/edits in their left buffers and generate some output 
 * (e.g. on the terminal or graphically), 
 * but do not put IUs onto their own right buffers.
 */
package inpro.incremental.sink;